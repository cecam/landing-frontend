'use client'

import useForm from "@/hooks/useForm";

const Form = () => {
    const [ state, handleChange ] = useForm({
        
    })

    return(
        <form>

        </form>
    )
}

export default Form;